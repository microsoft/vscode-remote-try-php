{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch application",
            "type": "php",
            "request": "launch",
            "program": "${workspaceFolder}/main.php",
            "cwd": "${workspaceFolder}",
            "port": 9000
        }
    ]
}